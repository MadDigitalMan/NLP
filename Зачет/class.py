class Point:    def __new__(cls, *args, **kwargs):        print('call __new__ for ' + str(cls))        return super().__new__(cls)    def __init__(self,x=0,y=0):       self.x=x       self.y=yclass Point1:    x1 = 0    x2 = 0    y1 = 0    y2 = 0    def __init__(self, x1,y1,x2,y2):        self.x1 = x1        self.y1 = y1        self.x2 = x2        self.y2 = y2    def info(self):        print(f'Координаты: {self.x1, self.y1, self.x2, self.y2}')point = Point(1,2)print(point)class Equipments():    def __init__(self, voltage, name):        self.voltage = voltage        self.name = name        status = False    def On(self):        self.status = True        print('Lamp is on', self.name)    def Off(self):        self.status = False        print('Lamp is Off', self.name)class Lamp(Equipments):    brightness = 0    def info(self):        print('Status:',self.status)        print('Bright',self.brightness)class Hero:    def __init__(self,hp,attack,speed):        self.hp = hp        self.attack = attack        self.speed = speed    def Speak(self):        print('Hero say')class SuperHero(Hero):    def Fly(self):        self.speed += 50    def Speak(self):        print("It's over 9000")class Enemy:    def __init__(self):        self.hp = 23        self.attack = 10        self.speed = 12    def Speak(self):        print("adsfadsfsa")class DataBase:    __instance = None    def __new__(cls, *args, **kwargs):        if cls.__instance is None:            cls.__instance = super().__new__(cls)        return cls.__instance    def __del__(self):        DataBase.__instance = None    def __init__(self, user, psw, port):        self.user =user        self.psw = psw        self.port = port    def connect(self):        print(f'Coonnect {self.user},{self.psw},{self.port}')    def close(self):        print('Close coonection with DB')    def read(self):        return 'Data from DB'    def write(self,data):        print(f'Write to {data}')class SimpleClass:    def __init__(self):        print('mdfomdopm')    def __del__(self):        print('Close')    def __add__(self, other):        print(f'dzfsdfsaf {other}' )class MyNum():    def __init__(self,x):        self.x=x    def __iadd__(self, other):        return self.x    def get_val(self):        return self.xdb = DataBase('Egor', 'sdfdf', '12-2121')db1 = DataBase('root', 'dafasf', 12)db.connect()db1.connect()def Arr(arr, n):    return list(arr[:n])a = [1,2,4,5,6,7,88,9,]print(Arr(a,6))